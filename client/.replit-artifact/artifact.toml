kind = "web"
previewPath = "/"
title = "Resume & JD Matcher"
version = "1.0.0"
id = "artifacts/resume-matcher"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25420

[services.development]
run = "pnpm --filter @workspace/resume-matcher run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/resume-matcher", "run", "build" ]
publicDir = "artifacts/resume-matcher/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25420"
BASE_PATH = "/"
